/**
 * 브랜드 토큰 — 캐러셀의 색, 글자 크기, 여백이 전부 여기서 나온다.
 *
 * ⚠️ 지금 값은 "속눈썹 아티스트 + 교육자 + 글로벌"이라는 방향만 보고 잡은 초안이다.
 *    참고 계정 무드보드가 나오면 이 파일만 고치면 캐러셀 전체가 한 번에 바뀐다.
 *    슬라이드 코드는 건드릴 필요 없다.
 */

export const BRAND = {
  /** 계정 이름 — 슬라이드 하단 서명에 쓴다 */
  handle: "@your_account",
  /** 영문 표기 — 해외 계정에 노출될 때 쓰인다 */
  nameEn: "Korean Lash Artist",
  /** 표지 맨 위에 작게 들어가는 분류. 시리즈를 나누고 싶으면 글마다 바꿔도 된다 */
  eyebrow: "LASH EDUCATION",

  colors: {
    /** 표지 배경. 짙은 먹색이 속눈썹·시술 사진과 가장 잘 붙는다 */
    ink: "#141210",
    /** 본문 배경. 완전한 흰색보다 눈이 편하고 고급스럽다 */
    paper: "#F7F4F0",
    /** 짙은 배경 위 글자 */
    onInk: "#F7F4F0",
    /** 밝은 배경 위 글자 */
    onPaper: "#1A1714",
    /** 강조 — 샴페인 골드. 뷰티에서 과하지 않게 고급을 만든다 */
    accent: "#C9A227",
    /** 보조 설명 글자 */
    muted: "#8A8178",
  },

  /** 캐러셀 규격. 4:5는 피드에서 세로로 가장 크게 잡히고 글도 많이 들어간다 */
  size: { width: 1080, height: 1350 },

  type: {
    /** 표지 제목 */
    hero: 92,
    /** 본문 제목 */
    title: 66,
    /** 본문 */
    body: 40,
    /** 영문 보조 */
    sub: 30,
    /** 페이지 번호·서명 */
    caption: 26,
  },

  /** 슬라이드 안쪽 여백. 인스타는 가장자리가 잘려 보이므로 넉넉히 준다 */
  padding: 88,
} as const;

export type Brand = typeof BRAND;
