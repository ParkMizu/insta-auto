/**
 * 브랜드 토큰 — 캐러셀의 색, 글자 크기, 여백이 전부 여기서 나온다.
 *
 * 값의 근거는 MOODBOARD.md에 있다. 요약하면:
 *  - @yoonble_studio 처럼 피드 전체를 뉴트럴 누드 한 톤으로 묶는다.
 *    시술 사진·일상 사진·글자 카드가 같은 색 온도에 있어야 피드가 안 깨진다.
 *  - 검정+골드(처음 초안)는 강하지만 일상 사진과 섞이지 않아서 버렸다.
 *  - 영문을 장식이 아니라 정보로 쓴다. 해외 계정이 스쳐도 무슨 내용인지 읽혀야 한다.
 */

export const BRAND = {
  handle: "@amiju____",
  /** 해외 계정에 노출될 때의 한 줄. 프로필 이름에도 같은 문구를 쓰는 게 좋다 */
  nameEn: "Korean Lash Artist · Seoul",
  /** 표지 맨 위 분류. 시리즈를 나누려면 글마다 바꿔도 된다 */
  eyebrow: "LASH EDUCATION",

  colors: {
    /** 표지 배경 — 짙은 모카. 검정보다 부드럽고 누드 톤과 한 가족이다 */
    deep: "#3B322C",
    /** 본문 배경 — 따뜻한 누드. 흰색이 아니라 살짝 앉은 베이지여야 피드가 고급스럽다 */
    nude: "#EDE5DC",
    /** 더 밝은 판 — 본문이 길 때 숨 쉬는 장 */
    light: "#F6F1EB",
    /** 짙은 배경 위 글자 */
    onDeep: "#F6F1EB",
    /** 밝은 배경 위 글자 */
    onNude: "#3B322C",
    /** 강조 — 로즈 누드. 골드보다 조용하고 뷰티에 붙는다 */
    accent: "#B98D7B",
    /** 보조 설명 */
    muted: "#9A8B7E",
    /** 짙은 배경에서의 보조 설명 */
    mutedOnDeep: "#B3A79B",
  },

  /** 4:5는 피드에서 세로로 가장 크게 잡히고 글도 많이 들어간다 */
  size: { width: 1080, height: 1350 },

  type: {
    /** 표지 한글 제목 */
    hero: 88,
    /** 표지 영문 — 한글 바로 위에서 같은 무게로 읽힌다 */
    heroEn: 34,
    /** 본문 제목 */
    title: 62,
    /** 본문 */
    body: 38,
    /** 영문 보조 */
    sub: 29,
    /** 페이지 번호·서명 */
    caption: 25,
  },

  padding: 88,
} as const;

export type Brand = typeof BRAND;
